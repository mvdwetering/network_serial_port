{
  "config": {
    "step": {
      "user": {
        "description": "Serial port can be a normal path or any [URL handler as supported by PySerial](https://pyserial.readthedocs.io/en/latest/url_handlers.html).\n\nThis can come in handy when addressing USB adapters by serial with `hwgrep://` or use `rfc2217://hostname_or_ip` to connect through an rfc2217 compatible server.",
        "data": {
          "serial_url": "Serial port",
          "baudrate": "Baudrate",
          "tcp_port": "TCP port to listen on"
        }
      }
    },
    "error": {
      "cannot_connect": "Cannot open serial URL or TCP port",
      "unknown": "Unknown error"
    },
    "abort": {
      "already_configured": "Device is already configured"
    }
  },
  "entity": {
    "binary_sensor": {
      "client_connected": {
        "name": "Client connection"
      }
    },
    "sensor": {
      "connected_client": {
        "name": "Client"
      }
    }
  }
}